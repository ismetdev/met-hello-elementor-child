# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.code-workspace

# Node / build (in case tooling is added later)
node_modules/
*.log

# Local env / secrets — never commit tokens
.env
*.local

# Claude Code personal/local settings
.claude/settings.local.json

# Build output from the release workflow
/build/
*.zip
